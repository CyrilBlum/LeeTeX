\lstset{style=Python,escapeinside={(*@}{@*)}, basicstyle=\ttfamily\footnotesize}
\input{Grundlagen_Info/00_Programmieren/Skript/Macros}

\begin{frame}
	\centering
	\begin{figure}[H]
		\includegraphics[keepaspectratio, width=0.7\paperwidth, height=\paperheight]{Logos/LeeLogo}
	\end{figure}
	Informatik: Programmieren\\
	\emoji{cook} Kapitel 4: \textbf{Funktionen mit \lstinline|return|}: Mehrere Funktionen
\end{frame}

\begin{frame}[fragile]{Funktionen: Mit \lstinline|return|}
	\begin{lstlisting}
def summiere(x1, x2):
    summe = x1 + x2
    return (*@\only<1>{summe}\only<2->{\colorbox{RoyalBlue!50}{sum\tikzmark{arrowFromEx01}me}}@*)
    


(*@\only<1-3>{res}\only<4->{\colorbox{ForestGreen!50}{re\tikzmark{arrowToEx02}s}}@*) = (*@\only<1>{summiere(3, 5)}\only<2->{\colorbox{RoyalBlue!50}{summie\tikzmark{arrowToEx01}re(3, 5)}}@*)

print(res)
\end{lstlisting}
	\begin{tikzpicture}[overlay,remember picture]
		% Wert zurückgeben
		\only<2>{
			\draw[RoyalBlue,-stealth, thick] (arrowFromEx01.south) to[bend left] node[right, xshift=.1cm, pos=.4, fill=RoyalBlue,fill opacity=.5, text opacity=1, text=black,rounded corners]{8}(arrowToEx01.north);}
		\only<3->{
			\draw[RoyalBlue,-stealth, thick] (arrowFromEx01.south) to[bend left] node[align=left, pos=.5, right, xshift=.1cm, fill=RoyalBlue,fill opacity=.5, text opacity=1, text=black,rounded corners] {Wert (8) an das Hauptprogramm zurückgeben...} (arrowToEx01.north);}

		\only<4->{
			\draw[ForestGreen,-stealth, thick] (arrowToEx01.south) to[bend left] node[align=left, pos=0, below right, fill=ForestGreen,fill opacity=.5, text opacity=1, text=black,rounded corners, yshift=-.1cm] {...\textit{und} Wert in Variable speichern, z.B. \lstinline|res|} (arrowToEx02.south);
		}
	\end{tikzpicture}
	\only<5->{
		\begin{center}
			Weshalb nicht einfach \lstinline|print| verwenden?
		\end{center}
	}
\end{frame}


\begin{frame}[fragile]{Funktionen}{Ein einfaches Beispiel}
	\begin{lstlisting}
def berechne_rabatt(preis, rabatt_pct):
    rabatt = preis * (rabatt_pct / 100)
    (*@\colorbox{CornflowerBlue}{return preis - \tikzmark{arrowFromEx11} rabatt}@*)


def berechne_gesamtpreis(preis, rabatt_pct, mwst):
    (*@\colorbox{CornflowerBlue}{rabatt\tikzmark{arrowToEx11}preis}@*) = berechne_rabatt(preis, rabatt_pct)
    mwst = rabattpreis * (mwst / 100)
    return (*@\colorbox{CornflowerBlue}{rabattpre\tikzmark{arrowFromEx12}is + mwst}@*) 


(*@\colorbox{CornflowerBlue}{endp\tikzmark{arrowToEx12}reis}@*) = berechne_gesamtpreis(100, 15, 7.7)
print("Der Preis nach Rabatt und Mwst ist", endpreis)
\end{lstlisting}
	\begin{tikzpicture}[overlay,remember picture]
		% Wert zurückgeben
		\only<2->{\draw[red,-stealth] (arrowFromEx11.north) to[bend left] node[pos=.3, right, fill=RoyalBlue,fill opacity=.5, text opacity=1, text=black,rounded corners] {Wert zurückgeben (und speichern)} (arrowToEx11);}
		\only<3->{\draw[red,-stealth] (arrowFromEx12.north) to[bend left] node[pos=.3, right, fill=RoyalBlue, fill opacity=.5, text opacity=1, text=black,rounded corners] {Wert zurückgeben  (und speichern)} (arrowToEx12);}
	\end{tikzpicture}

\end{frame}

\begin{frame}[fragile]{Funktionen: Beispiel 2}
	\begin{lstlisting}
def berechne_rechteck_flaeche((*@\only<1>{laenge, breite}\only<2->{\colorbox{CarnationPink}{laenge, breite}}@*)):
    flaeche = laenge * breite
    return (*@\only<1-2>{flaeche}\only<3->{\colorbox{CornflowerBlue}{flaeche}}@*)

def ist_grosse_flaeche((*@\only<1>{flaeche,schwellenwert}\only<2->{\colorbox{CarnationPink}{flaeche, schwellenwert}}@*)):
    return (*@\only<1-2>{flaeche > schwellenwert}\only<3>{\colorbox{CornflowerBlue}{flaeche > schwellenwert}}@*)

# Berechne die Fläche
(*@\only<1-2>{flaeche}\only<3->{\colorbox{CornflowerBlue}{flaeche}}@*) = berechne_rechteck_flaeche((*@\only<1>{15, 8}\only<2->{\colorbox{CarnationPink}{15, 8}}@*))

# Überprüfe, ob die Fläche grösser als 50 ist
(*@\only<1-2>{test}\only<3->{\colorbox{CornflowerBlue}{test}}@*) = ist_grosse_flaeche((*@\only<1>{flaeche, 50}\only<2->{\colorbox{CarnationPink}{flaeche, 50}}@*))
if test:
    print("Fläche des Rechtecks ist grösser als 50.")
else:
    print("Fläche des Rechtecks ist kleiner 50.")
\end{lstlisting}
	\only<1>{Wo sind \colorbox{CarnationPink}{Parameter?}}
	\only<2>{Wo sind \colorbox{CornflowerBlue}{Return-Werte?}}
\end{frame}


\begin{frame}[fragile]{\textbf{Modularität} durch Funktionen}
	\begin{figure}[H]
		% Use the \drawCube macro to draw various cubes
		\centering
		\resizebox{.7\linewidth}{!}{
			\begin{tikzpicture}
				\drawCube{1}{0}{2}{3}
				\node[fill=blue!50, rounded corners] (input1) at (llD11val){3};
				\node[fill=blue!50, rounded corners] (input2) at (llD21val){12};
				\draw[thick,->] (input1) -- (in11);
				\draw[thick,->] (input2) -- (in12);
				\node[fill=green!50, rounded corners](outVal1) at (5,-.6,3){36};
				\draw[thick,->] (out1) -- (outVal1);

				\node[fill=red!50, rounded corners](outVal1) at (1.5,1.5,1.5){\emoji{cook}};
			\end{tikzpicture}
		}
	\end{figure}
\end{frame}

\begin{frame}[fragile]{\textbf{Modularität} durch Funktionen}
	\resizebox{\linewidth}{!}{
		\begin{tikzpicture}
			\drawCube{1}{0}{2}{3}
			% feed input values into a1
			\node[fill=blue!50, rounded corners] (input1) at (llD11val){3};
			\node[fill=blue!50, rounded corners] (input2) at (llD21val){12};
			\draw[thick,->] (input1) -- (in11);
			\draw[thick,->] (input2) -- (in12);
			\node[fill=green!50, rounded corners](outVal1) at (5,-.6,3){36};
			\draw[thick,->] (out1) -- (outVal1);
			\drawCube{2}{5.25}{3}{3}
			\drawCube{3}{10.5}{2}{3}
			\draw[thick,->,rounded corners] (out1) -- ++(0,-3.25,0) -- ++(3.15,0,0)node[below]{\lstinline[language=Python,style=python,basicstyle=\ttfamily]{return out1}} -- ++(4.65,0,0) -- (in31);
			\node[fill=red!50, rounded corners](outVal1) at (1.5,1.5,1.5){\emoji{cook}};
			\node[fill=ForestGreen!50, rounded corners](outVal3) at (6.75,1.5,1.5){\emoji{cook}};
			\node[fill=black!50, rounded corners](outVal2) at (12,1.5,1.5){\emoji{cook}};
		\end{tikzpicture}}
	\only<2->{
		\begin{center}
			Auch dieses Bild wurde mit einer Funktion erstellt...
		\end{center}}
\end{frame}

\begin{frame}[fragile]{\textbf{Modularität} durch Funktionen}
	\begin{tikzpicture}[
			node distance=1cm,
			process/.style={rectangle, rounded corners, minimum height=0.8cm, text centered, fill=blue!50},
			arrow/.style={->, >=Stealth, thick}
		]

		% Nodes
		\node (input) [process] {Eingabe};

		\node (func1) [process, right=of input] {F1};
		\node (func2) [process, right=of func1] {F2};
		\node (funcdots) [right=of func2] {$\cdots$};
		\node (funcn) [process, right=of funcdots] {Fn};

		\node (output) [process, right=of funcn] {Ausgabe};

		% Arrows
		\draw [arrow] (input) -- (func1);
		\draw [arrow] (func1) -- (func2);
		\draw [arrow] (func2) -- (funcdots);
		\draw [arrow] (funcdots) -- (funcn);
		\draw [arrow] (funcn) -- (output);

	\end{tikzpicture}
	\textbf{Analogien}:
	\begin{itemize}
		\item Uhren-Fabrik
		\item Michelin-Küche
		\item ...alle komplexen Prozesse, die man in Unter-Prozesse aufbrechen muss!
	\end{itemize}
	\textbf{Anwendungen}: Überall! Daten-Analyse, AI, Business Development etc.
\end{frame}

\begin{frame}[fragile]{Auftrag}{Programmier-Skript, Kapitel 4}

	\begin{itemize}
		\item \aufgabebox{Aufgaben 4.10-4.12}
		\item \aufgabebox{Moodle, Kapitel 4 (Teil 1)}
		\item \aufgabebox{Moodle, Kapitel 4 (Teil 2)}
		\item \aufgabebox{Moodle, Kapitel 4 (Zusammenfassung)}
		\item \challengebox{Moodle, Kapitel 4 (Challenges)}
	\end{itemize}
\end{frame}