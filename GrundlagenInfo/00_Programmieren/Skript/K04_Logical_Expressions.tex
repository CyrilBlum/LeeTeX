\chapter{Verzweigungen und Logische Ausdrücke}\label{ch:K03_logical}

\section{Verzweigungen mit \texorpdfstring{\lstinline|if|}{if}, \texorpdfstring{\lstinline|elif|}{elif} und \texorpdfstring{\lstinline|else|}{else}}
In vielen Fällen möchten wir Code nur Ausführen, \textit{falls} eine gewisse Bedingung wahr ist: beispielsweise möchte ein Arzt nur dann eine Warnung erhalten, wenn der Blutdruck eines Patienten zu hoch ist, oder ein selbstfahrendes Auto sollte nur dann piepsen, wenn der Fahrer nicht aufmerksam auf die Strasse schaut. Um solche \textit{konditionale} (bedingte) Logik zu programmieren, können wir die Begriffe \lstinline|if| (\enquote{falls}), \lstinline|elif| (\enquote{sonst falls}) und \lstinline|else| (\enquote{in allen anderen Fällen}) verwenden.

\subsection{Verzweigungen mit \texorpdfstring{\lstinline|if|}{if}}

\begin{myexample}\label{ex:if}
Folgendes Beispiel führt nur zu einem Output, falls eine Temperatur von 30 Grad oder mehr eingegeben wird.
\lstinputlisting[language=Python]{GrundlagenInfo/00_Programmieren/Skript/Code/K04_Logic/ex_if.py}

Der Code kann folgendermassen als Fluss-Diagramm aufgezeichnet werden (s. \cref{fig:if-flussdiagramm}).
Auf dem Flussdiagramm werden die einzelnen Schritte des Codes als Boxen dargestellt. Die Boxen sind durch Pfeile verbunden, die den Fluss des Codes darstellen. Die Entscheidungspunkte sind durch Rauten dargestellt, und die Pfeile zeigen, welche Aktion ausgeführt wird, je nachdem, ob die Bedingung wahr oder falsch ist.

\begin{figure}[H]
	\centering
	\input{GrundlagenInfo/00_Programmieren/Skript/Figures/k04-if.tex}
	\caption{Flussdiagrammm für den Code aus \cref{ex:if}}
	\label{fig:if-flussdiagramm}
\end{figure}
Dabei bezeichnen rote Boxen eine Zeile Code, grüne Formen einen Test der schwarze Punkt das Programmende.
\end{myexample}

\begin{myexercise}
	Entwickeln Sie eine Funktion \lstinline|def quadrat(laenge)| zum Zeichnen von Quadraten. Der Befehl soll aber nur etwas zeichnen, wenn die Seitenlänge mindestens 40 beträgt.
	\begin{myanswer}
		\lstinputlisting[language=Python,caption=\texttt{quadrat40.py},label=listing:quadrat40]{GrundlagenInfo/00_Programmieren/Skript/Code/K04_Logic/quadrat40.py}
	\end{myanswer}
\end{myexercise}

\begin{myexercise}
	Entwickeln Sie ein Programm, das dem Benutzer im Rahmen einer interaktiven Fragerunde drei Fragen stellt (mit dem Befehl \lstinline|input("...")|). Das Programm soll die Anzahl der richtigen Antworten
	zählen und diese Anzahl ausgeben.
	\begin{myanswer}
		\lstinputlisting[language=Python,caption=\texttt{fragerunde.py},label=listing:fragerunde]{GrundlagenInfo/00_Programmieren/Skript/Code/K04_Logic/fragerunde.py}
	\end{myanswer}
\end{myexercise}


\subsection{Verzweigungen mit \texorpdfstring{\lstinline|if|}{if} und \texorpdfstring{\lstinline|else|}{else}}

\begin{myexample}
In folgendem Code wird mit dem \lstinline|else|-Ausdruck ein Fall definiert, welcher ausgeführt wird, sofern die \lstinline|if|-Kondition nicht zutrifft.
\lstinputlisting[language=Python]{GrundlagenInfo/00_Programmieren/Skript/Code/K04_Logic/ex_if_else.py}

Der Code kann folgendermassen als Fluss-Diagramm aufgezeichnet werden:
\begin{figure}[H]
	\centering
	\input{GrundlagenInfo/00_Programmieren/Skript/Figures/k04-if-else.tex}
\end{figure}
\end{myexample}

\subsection{Verzweigungen mit \texorpdfstring{\lstinline|if|}{if}, \texorpdfstring{\lstinline|elif|}{elif} und \texorpdfstring{\lstinline|else|}{else}}
\begin{myexample}
Folgender Code führt eine von drei Möglichkeiten aus:
\lstinputlisting[language=Python]{GrundlagenInfo/00_Programmieren/Skript/Code/K04_Logic/ex_if_elif_else.py}

Der Code kann folgendermassen als Fluss-Diagramm aufgezeichnet werden:

\begin{figure}[H]
	\centering
	\input{GrundlagenInfo/00_Programmieren/Skript/Figures/k04-if-elif-else.tex}
\end{figure}
\end{myexample}

\begin{mysubmission}
	Ein Bäcker möchte Kekse backen und gleichmässig in Keksdosen verpacken, so dass jede Dose voll ist. Jede Dose fasst 12 Kekse. Schreibe ein Programm, das berechnet:

	\begin{itemize}
		\item Wie viele Dosen benötigt werden für n Kekse.
		\item Wie viele Kekse übrig bleiben.
	\end{itemize}

	Falls mehr als 500 oder weniger als 1 für \texttt{n} eingegeben werden, soll ausgegeben werden:  \lstinline|"Ungültige Anzahl Kekse!"|

	Verwenden Sie die Ganzzahldivision (\lstinline|//|) und Modulo (\lstinline|%|)!

	\begin{myanswer}
		\lstinputlisting[language=Python,caption=\texttt{kekse.py},label=listing:kekse]{GrundlagenInfo/00_Programmieren/Skript/Code/K04_Logic/kekse.py}
	\end{myanswer}
\end{mysubmission}

\begin{myexercise}
	Verwenden Sie den Code aus \cref{ex:multiple-params}.
	Verwenden Sie einen \lstinline|input|-Befehl aus \cref{ex:input}, um den Benutzer nach einer Zahl zu fragen.
	\begin{enumerate}
		\item Falls die Zahl 1 eingegeben wird, soll ein blaues Viereck gezeichnet werden
		\item Falls eine Zahl von 2 bis und mit 5 eingegeben wird, soll ein grünes Sechseck gezeichnet werden
		\item Falls eine Zahl $n$ von 6 oder grösser eingegeben wird, soll ein schwarzes $n$-Eck gezeichnet werden.
	\end{enumerate}
\end{myexercise}


\begin{mysubmission}
	Bei einer Flugreise darf der aufgegebene Koffer üblicherweise nicht schwerer sein als 20kg. Ansonsten bezahlt man einen Aufschlag von CHF 5.- pro kg Übergewicht.

	Schreiben Sie ein Unterprogramm koffer(gewicht), der einen Parameter Gewicht entgegennimmt und einen Text auf der Konsole druckt, je nach Fall:

	\begin{itemize}
		\item Wenn der Koffer über 100 kg wiegt, wird er nicht transportiert (\lstinline|"Der Koffer ist zu schwer"|)
		\item Wenn der Koffer über 20 kg und bis und mit 100 kg wiegt, muss der Aufpreis berechnet und ausgegeben werden (z.B. \lstinline|"Ihr Koffer hat X kg Übergewicht. Das kostet 5.-"|)
		\item Wenn der Koffer über 0 kg und bis und mit 20 kg wiegt, muss kein Aufpreis bezahlt werden und es wird gedruckt \lstinline|"Der Koffer ist gratis"|
		\item  In allen anderen Fällen soll ausgegeben werden ``Das eingegebene Gewicht ist nicht zulässig''  (0, negative Zahlen etc.).
	\end{itemize}
	\begin{myanswer}
		\lstinputlisting[language=Python,caption=\texttt{koffer.py},label=listing:koffer]{GrundlagenInfo/00_Programmieren/Skript/Code/K04_Logic/koffer.py}
	\end{myanswer}
\end{mysubmission}

\begin{myexercise}
	Entwickeln Sie eine Funktion \lstinline|def vielecke_sicher(anzahl, seite)| zum Zeichnen von regelmässigen Vielecken mit wählbarer Anzahl Ecken und wählbarer Seitenlänge. Wenn \lstinline|anzahl| (die Anzahl der Ecken) kleiner als 1 ist, soll das Programm nichts tun. Wenn \lstinline|anzahl == 1| ist, soll das Programm
	\lstinline|"Es gibt kein 1-Eck"| ausgeben. Wenn \lstinline|anzahl == 2| ist, soll das Programm
	\lstinline|"Es gibt kein 2-Eck"| ausgeben. Wenn \lstinline|anzahl >= 3| ist, soll das Programm das \lstinline|anzahl|-Eck mit Seitenlänge \lstinline|seite| zeichnen. Zeichnen Sie auch das dazugehörige Flussdiagramm.
	\begin{myanswer}
		\lstinputlisting[language=Python,caption=\texttt{vieleck\_sicher.py},label=listing:vieleck_sicher]{GrundlagenInfo/00_Programmieren/Skript/Code/K04_Logic/vieleck_sicher.py}
	\end{myanswer}
\end{myexercise}

\begin{myexercise}
	Entwickeln Sie eine Funktion \lstinline|def quadgleich(a, b, c)|, der quadratische Gleichungen der Form \(ax^2 + bx + c = 0\) löst. Der Befehl soll zuerst \(d = b^2 - 4ac\) ausrechnen und abhängig vom Wert von $d$ keine, eine oder zwei Lösungen ausgeben.
	\begin{myanswer}
		\lstinputlisting[language=Python,caption=\texttt{quadgleich.py},label=listing:quadgleich]{GrundlagenInfo/00_Programmieren/Skript/Code/K04_Logic/quadgleich.py}
	\end{myanswer}
\end{myexercise}

\begin{myexercise}
	Was gibt dieser Code aus? Ist die Ausgabe sinnvoll? Weshalb (nicht)?
	\lstinputlisting[language=Python]{GrundlagenInfo/00_Programmieren/Skript/Code/K04_Logic/ex_if_if_else.py}
	\begin{myanswer}
		Der Code gibt sowohl \lstinline|"Es ist heiss"| als auch \lstinline|"Es ist warm"| aus. Dies ist nicht sinnvoll, da die Temperatur 33 Grad beträgt und somit nur eine der beiden Aussagen wahr sein kann.

		Der Grund dafür ist, dass die zweite \lstinline|if|-Bedingung unabhängig von der ersten \lstinline|if|-Bedingung ist. Das \lstinline|else| gehört nur zur zweiten \lstinline|if|-Bedingung. Das bedeutet, dass die zweite \lstinline|if|-Bedingung immer ausgeführt wird, selbst wenn die erste \lstinline|if|-Bedingung bereits wahr war. Aus diesem Grund wäre es sinnvoller, statt einem \lstinline|if|-Befehl eine \lstinline|elif|-Bedingung zu verwenden.
	\end{myanswer}
\end{myexercise}

\section{Logische Ausdrücke}
\tib{Logische Ausdrücke}, auch \tib{Boolsche Ausdrücke} sind Ausdrücke, die genau zwei verschiedene Werte annehmen können: entweder \textit{wahr} (\lstinline|True|) oder \textit{falsch} (\lstinline|False|). Beispiele sind:
\begin{lstlisting}
	3 == 5 # gibt den Wert False zurück
	(9 + 3) < (2 * 8) # gibt den Wert True zurück
\end{lstlisting}

Wie Sie in den obigen Beispielen sehen, wird von einem Wahrheitstest ein \textbf{Wert} zurückgegeben: entweder der Wert \lstinline|True| (Wahr) oder \lstinline|False| (Falsch).

Mögliche logische Relationen sind in \cref{table:bool} abgebildet.
\begin{table}[H]
	\centering
	\begin{tabular}{c||c}
		\textbf{Python} & \textbf{Mathematische Bedeutung} \\\midrule
		\lstinline|==|  & gleich ($=$)                     \\
		\lstinline|!=|  & ungleich ($\neq$)                \\
		\lstinline|<|   & kleiner ($<$)                    \\
		\lstinline|<=|  & kleiner oder gleich ($\leq$)     \\
		\lstinline|>|   & grösser ($>$)                    \\
		\lstinline|>=|  & grösser oder gleich ($\geq$)
	\end{tabular}
	\caption{Logische Relationen und Schreibweise in Python}
	\label{table:bool}
\end{table}
Logische (oder auch bool'sche) Audrücke wie z.B. \lstinline|3 < 45| führen zu einem \tib{Bool'schen Wert}, also einem Wahrheitswert, welcher entweder den Wert \lstinline|True| (wahr) oder \lstinline|False| (falsch) hat.

Die typische Verwendungsart logischer Ausdrücke ist in folgendem Code abgebildet:
\begin{lstlisting}
	if BOOLSCHER_WERT:
		# dieser Code wird nicht ausgeführt, wenn BOOLSCHER_WERT falsch ist (False)
\end{lstlisting}

\begin{myexercise}
	Entwickeln Sie eine Funktion, die Bilder abhängig von der Eingabe zeichnet. Wenn die Benutzerin eine 1 eingibt, soll der Befehl ein rotes Rechteck zeichnen. Bei einer 2 soll er ein grünes Dreieck zeichnen. Bei einer 3 soll er ein blaues Sechseck zeichnen. Bei allen anderen Eingaben soll der Befehl den Text \enquote{Nur die Zahlen 1, 2 oder 3 werden als Eingabe akzeptiert.} ausgeben.
	\begin{myanswer}
		\lstinputlisting[language=Python,caption=\texttt{vieleck\_if\_elif.py},label=listing:vieleck_if_elif]{GrundlagenInfo/00_Programmieren/Skript/Code/K04_Logic/vieleck_if_elif.py}
	\end{myanswer}
\end{myexercise}

\begin{mysubmission}
	Geben seien zwei Zahlen \lstinline|x1| und \lstinline|x2|. Schreiben Sie eine Funktion def \lstinline|maxzahl(x1, x2)|, welche die grössere der beiden Zahlen mit \lstinline|return| zurück an das Hauptprogramm gibt. Wenn beide Zahlen gleich gross sind, soll \lstinline|x2| zurückgegeben werden.

	Überprüfen Sie die Korrektheit Ihrer Funktion, indem Sie sie auf Moodle hochladen.
	\begin{myanswer}
		\lstinputlisting{GrundlagenInfo/00_Programmieren/Skript/Code/K04_Logic/maxzahl.py}
	\end{myanswer}
\end{mysubmission}

\begin{mychallenge}
	Lösen Sie die Aufgabe ``Dynamisches Schachbrett'' auf Moodle.
\end{mychallenge}

\subsection{Logische Ausdrücke miteinander verbinden}



Häufig fällen wir im echten Leben Entscheidungen, welche nicht nur von einer Bedingung abhängen, sondern gleich von mehreren, so zum Beispiel:
\begin{itemize}
	\item Ich gehe per Fahrrad zur Schule, \textit{falls} das Wetter schön ist \textit{und} ich mich körperlich fit fühle.
	\item Ich esse etwas, \textit{falls} ich Hunger habe \textit{oder} ich Lust darauf habe (auch wenn ich keinen Hunger habe).
\end{itemize}
Beim ersten Beispiel handelt es sich um eine Verbindung per ``\textit{und}'': beide Konditionen müssen wahr sein, damit etwas geschieht. Dies kann in Python mit \lstinline|and| (englisch für ``und'') umgesetzt werden.

\begin{myexample}\label{example:fahrrad}
Folgendes Beispiel illustriert, wie mehrere Bedingungen miteinander verknüpft werden können:
\begin{lstlisting}
		temperature = 25 # aktuelle Aussen-Temperatur
		fitness = 80 # körperliche Fitness (zwischen 0-100, subjektiv empfunden)
		if (temperature > 20) and (fitness > 80):
			print("Ich gehe per Fahrrad zur Schule!")
	\end{lstlisting}
\end{myexample}

Beim zweiten Beispiel handelt es sich um eine Verbindung mehrerer Bedingungen per ``\textit{oder}'': es reicht, dass eine von beiden Bedingungen wahr ist, damit etwas geschieht. Dies kann in Python mittels dem Wort \lstinline|or| (englisch für ``oder'') umgesetzt werden.
\begin{myexample}
Folgendes Beispiel illustriert, wie mehrere Bedingungen miteinander verknüpft werden können:
\begin{lstlisting}
		hunger = 25 # aktueller Hunger-Wert (zwischen 0-100, subjektiv empfunden)
		lecker = 80 # wie lecker ist das Lebensmittel (Skala von 0 bis 100)?
		if (hunger > 80) or (lecker > 80):
			print("Ich will das essen!")
	\end{lstlisting}
\end{myexample}

\begin{myexercise}\label{ex:geschwindigkeitAngemessen}
	Schreiben Sie eine Funktion \lstinline|def geschwindigkeit_angemessen(geschwindigkeit):|, welche als Parameter \lstinline|geschwindigkeit| eine Zahl entgegennimmt (z.B. \lstinline|50| oder \lstinline|120|). Falls die Geschwindigkeit zwischen 30 und 100 km/h liegt (einschliesslich dieser Werte), soll auf der Konsole ausgegeben werden: ``Die Geschwindigkeit ist angemessen''. Ansonsten soll ausgegeben werden ``Die Geschwindigkeit ist nicht angemessen''. Verwenden Sie dazu den Ausdruck \lstinline|and|.
	\begin{myanswer}
		\lstinputlisting[language=Python,caption=\texttt{geschwindigkeit\_angemessen.py}]{GrundlagenInfo/00_Programmieren/Skript/Code/K04_Logic/geschwindigkeit_angemessen.py}
	\end{myanswer}
\end{myexercise}

\begin{myexercise}
	Schreiben Sie eine Funktion \lstinline|def temperatur_ist_unangenehm(temperatur):|, welche einen Parameter \lstinline|temperatur| als Zahl entgegennimmt. Falls die Temperatur kleiner als 10 Grad oder grösser als 30 Grad ist, soll auf der Konsole ausgegeben werden: ``Unangenehme Temperatur''. Ansonsten soll ausgegeben werden: ``Angenehme Temperatur''. Verwenden Sie dazu den Ausdruck \lstinline|or|.

	\begin{myanswer}
		\lstinputlisting[language=Python,caption=\texttt{temperatur.py}]{GrundlagenInfo/00_Programmieren/Skript/Code/K04_Logic/temperatur.py}
	\end{myanswer}
\end{myexercise}

\begin{myexercise}\label{ex:posUndGerade}
	Schreiben Sie eine Funktion \lstinline|def positiv_und_gerade(zahl):|, welche einen Parameter \lstinline|zahl| entgegennimmt, und testet, ob die Zahl positiv \textit{und} gerade ist, und falls dies zutrifft, den Text ausgibt \lstinline|"Die Zahl ist positiv und gerade"|. Ansonsten soll nichts ausgegeben werden.

	Zur Erinnerung: Eine Zahl ist gerade, wenn sie durch 2 teilbar ist. Dies kann in Python mit dem Modulo-Operator \lstinline|%| getestet werden. Beispiel: \lstinline|zahl % 2 == 0| gibt \lstinline|True| zurück, wenn die Zahl gerade ist.


	\begin{myanswer}
		\lstinputlisting[language=Python,caption=\texttt{pos\_und\_gerade.py}]{GrundlagenInfo/00_Programmieren/Skript/Code/K04_Logic/pos_und_gerade.py}
	\end{myanswer}
\end{myexercise}

\begin{myexercise}
	Entwickeln Sie eine Funktion \lstinline|def vieleck_kreis(anzahl_ecken, umfang):|, die ein Vieleck zeichnet. Damit das Vieleck aussieht wie ein Kreis, soll es nur gezeichnet werden, wenn die Anzahl der Ecken grösser als 35 ist und wenn der Umfang mindestens 100 ist.
	\begin{myanswer}
		\lstinputlisting[language=Python,caption=\texttt{vieleck\_kreis\_and.py}]{GrundlagenInfo/00_Programmieren/Skript/Code/K04_Logic/vieleck_kreis_and.py}
	\end{myanswer}
\end{myexercise}

\begin{mychallenge}
	Entwickeln Sie ein Programm, das alle natürlichen Zahlen zwischen 0 und 100 auf den Bildschirm schreibt, die durch 7, aber nicht durch 3 teilbar sind.
	\begin{myanswer}
		\lstinputlisting[language=Python,caption=\texttt{teiler\_7\_nicht3.py}]{GrundlagenInfo/00_Programmieren/Skript/Code/K04_Logic/teiler_7_nicht3.py}
	\end{myanswer}
\end{mychallenge}

\begin{mychallenge}
	Das harmonische Mittel zweier Zahlen a und b ist $\frac{2}{\frac{1}{a}+\frac{1}{b}}$. Es lässt sich aber nur berechnen, wenn weder $a$ noch $b$ null sind. Entwickeln Sie eine Funktion \lstinline|def harmonisches_mittel(a, b)|, die für die Parameter $a$ und $b$ das harmonische Mittel ausrechnet, wenn sowohl $a$ als auch $b$ nicht null sind. Ansonsten gibt der Befehl den Text \lstinline|"Das kann man nicht berechnen."| aus.

	Das harmonische Mittel ist eine wichtige Grösse in der Informatik, da es in vielen Algorithmen verwendet wird, beispielsweise in der Berechnung von Durchschnittswerten.

	Beispiel: Wenn Sie 100 Kilometer mit 50 km/h und 100 km/h fahren, beträgt die Durchschnittsgeschwindigkeit nicht 75 km/h, sondern 66.67 km/h. Das harmonische Mittel kann in diesem Fall verwendet werden, um die Durchschnittsgeschwindigkeit zu berechnen.

	\begin{myanswer}
		\lstinputlisting[language=Python,caption=\texttt{harmonisches\_mittel.py}]{GrundlagenInfo/00_Programmieren/Skript/Code/K04_Logic/harmonisches_mittel.py}
	\end{myanswer}
\end{mychallenge}

\begin{mychallenge}
	Schreiben Sie eine Python-Funktion \lstinline|def ist_schaltjahr(jahr)|, welche prüft, ob ein gegebenes Jahr ein Schaltjahr ist oder nicht.

	Ein Jahr ist ein Schaltjahr, genau dann wenn gilt:
	\begin{enumerate}
		\item (das Jahr ist durch 400 teilbar) oder
		\item (das Jahr ist durch 4 teilbar aber nicht durch 100)
	\end{enumerate}
	Für die Prüfung auf Teilbarkeit sollen Sie den Modulo-Operator (\lstinline|%| verwenden).
	\begin{myanswer}
		\lstinputlisting[language=Python,caption=\texttt{schaltjahr.py},label=listing:schaltjahr]{GrundlagenInfo/00_Programmieren/Skript/Code/K04_Logic/schaltjahr.py}
	\end{myanswer}
\end{mychallenge}

\subsection{Logische Ausdrücke negieren}
\textit{Negieren} bedeutet in der Informatik \textit{nicht}, das Gegenteil einer (urpsrünglichen) Aussage zu machen, sondern \textit{alle Aussagen zu machen}, welche durch die ursprüngliche Aussage nicht gemacht wurden, also alles ``andere'' als die ursprüngliche Aussage zu sagen.

\begin{myexample} Folgende Tabelle illustriert, was mit der Negation einer Aussage gemeint ist.
\begin{table}[H]
	\centering
	\begin{tabular}{p{7cm}|p{7cm}}
		\textbf{Aussage}                            & \textbf{Negation}                                          \\\midrule\midrule
		``Das Mädchen heisst Elin''                 & ``Das Mädchen heisst nicht Elin''                          \\\midrule
		``Niemand in dieser Klasse ist volljährig'' & ``Mindestens eine Person in dieser Klasse ist volljährig'' \\\midrule
		$x > 2$                                     & $x \leq 2$
	\end{tabular}
\end{table}
\end{myexample}
\begin{myexercise}
	(Von Hand) Notieren Sie zu folgenden Aussagen die Negation, \textit{ohne} die Wörter ``nicht'' oder ``kein'' zu verwenden:
	\begin{itemize}
		\item $x \geq 3$
		\item $y$ ist eine negative Zahl
		\item in der Variable \lstinline|test| ist der Wert \lstinline|"Franz"| gespeichert.
		\item Im Auto sitzen mindestens drei Menschen
		\item Der Koffer ist leer
		\item Das Programm ist falsch geschrieben
		\item Die Anzahl der Jugendlichen in der Klasse ist genau 19
		\item Morgen wird es in Zürich mindestens 22 Grad Celsius warm
	\end{itemize}
	\begin{myanswer}
		\begin{itemize}
			\item $x < 3$
			\item $y$ ist eine positive Zahl \textit{oder Null}
			\item in der Variable \lstinline|test| ist etwas Anderes als der Wert \lstinline|"Franz"| gespeichert.
			\item Im Auto sitzen weniger als drei Menschen
			\item Der Koffer enthält mindestens einen Gegenstand
			\item Das Programm ist richtig geschrieben
			\item Die Anzahl der Jugendlichen in der Klasse ist kleiner oder grösser als 19
			\item Morgen wird es in Zürich weniger als 22 Grad Celsius warm
		\end{itemize}
	\end{myanswer}
\end{myexercise}
Die Negation einer Aussage kann in Python mit dem Ausdruck \lstinline|not| (englisch für ``nicht'') gemacht werden. Die Negation einer Aussage (mit \lstinline|not|) ist insbesondere praktisch, um die Negation einer Aussage zu machen, ohne die Aussage komplett umschreiben zu müssen.

\begin{myexample}
Wir können den Code aus \cref{example:fahrrad} einfach mit dem Ausdruck \lstinline|not| umschreiben, um auszugeben, unterwelchen Bedingungen wir \textit{nicht} per Fahrrad zu Schule gehen wollen:
\begin{lstlisting}
if not((temperature > 20) and (fitness > 80)):
	print("Ich gehe nicht per Fahrrad zur Schule!")
\end{lstlisting}
Man könnte dieselbe Aussage auch folgendermassen formulieren: ``Falls ich mich nicht fit fühle \textit{oder} das Wetter schlecht ist, gehe ich nicht per Fahrrad zur Schule''. Dies sähe in Python folgendermassen aus:
\begin{lstlisting}
if (temperature <= 20) or (fitness <= 80):
	print("Ich gehe nicht per Fahrrad zur Schule!")
\end{lstlisting}
Weshalb haben wir im ersten Code ein \lstinline|and| und im zweiten Code ein \lstinline|or|? Dank dem \lstinline|not| müssen wir nichts vom ursprünglichen Code in \cref{example:fahrrad} umformulieren, da wir mit dem \lstinline|not| einfach alljene Fälle negieren, welche innerhalb der Klammer stehen. Somit kommen beide Codes zum selben Resultat.
\end{myexample}

\begin{myexercise}
	Schreiben Sie Ihren Code aus \cref{ex:posUndGerade} so um, dass getestet wird, ob eine Zahl weder gerade noch positiv ist. Verwenden Sie dazu unter anderem den Ausdruck \lstinline|not|. Testen Sie ihre Funktion für die Werte -3, +3, -4 und +4.
	Schreiben Sie damals dieselbe Funktion nochmals, ohne den Ausdruck \lstinline|not| zu verwenden.
	\begin{myanswer}
		\lstinputlisting[language=Python,caption=\texttt{weder\_pos\_noch\_gerade.py}]{GrundlagenInfo/00_Programmieren/Skript/Code/K04_Logic/weder_pos_noch_gerade.py}
	\end{myanswer}
\end{myexercise}

\section{Fussgesteuerte Schleifen mit \texorpdfstring{\lstinline|break|}{break}}

Bisher haben wir eine Art von Schleife gesehen: \lstinline|for _ in range(...)|. Dabei gibt die Zahl innerhalb des Befehls \lstinline|range(...)| an, wie viele Mal der Schleifenkörper wiederholt wird. Manchmal wissen wir jedoch nicht im voraus, wie viele Male eine Schleife wiederholt werden soll, wir kennen jedoch eine Bedingung, bei der die Schleife abgebrochen werden soll. Dies könnte beispielsweise der Fall sein, wenn wir eine Spirale zeichnen wolle, die immer grösser wird, bis die Seitenlänge eine gewisse maximale Länge \lstinline|max_seite| erreicht hat.



\begin{figure}[H]
	\centering
	\includegraphics[width=.2\linewidth]{GrundlagenInfo/00_Programmieren/Figures/spirale_while}
	\caption{Bild einer Spirale, deren grösste Seitenlänge \lstinline|max_seite| lang ist}
\end{figure}

Natürlich könnte man auch hier berechnen, wie viele Male die \lstinline|for|-Schleife ausgeführt werden muss. Die Formel, um dies zu berechnen, wäre:
\[\floor{\frac{\text{\texttt{maxseite - seite}}}{\text{\texttt{add}}}} + 1\]

Es geht allerdings auch einfacher, indem wir eine ``unendliche'' Schleife starten, die wir abbrechen, sobald eine gewisse Kondition wahr ist.

Grundsätzlich können wir auch direkt die Werte \lstinline|True| (Wahr) oder \lstinline|False| (Falsch) in Logischen Ausdrücken verwenden:

\begin{myexample}
Der \lstinline|print|-Befehl in folgendem Beispiel wird immer ausgeführt:
\begin{lstlisting}
		if True:
			print("Hello World")
	\end{lstlisting}
\end{myexample}

Den Wert \lstinline|True| könnten wir beispielsweise verwenden, um eine Endlosschleife mit \lstinline|while True:| zu konstruieren.


\begin{minipage}{\linewidth}
	\begin{lstlisting}[escapechar=!]
		import turtle as t
		
		# Tempo der Turtle festlegen
		t.speed(100)
		
		# gleichseitiges Dreieck zeichnen
		seite = 5
		max_seite = 50
		increment = 5
		while True:!\tikzmark{whileFrom}!                        !\tikzmark{while}!
			if seite > max_seite:
				break!\tikzmark{breakFrom}!                     !\tikzmark{break}!
			t.fd(seite)
			t.rt(90)
			seite += increment
			
		# Turtle-Zeichnung stehen lassen
		t.done()
	\end{lstlisting}
	\begin{tikzpicture}[overlay, remember picture,
			every node/.style={text=white,rounded corners}]
		\drawArrow{whileFrom}{while}{\footnotesize Unendliche Schleife}{DeepPink}
		\drawArrow{breakFrom}{break}{\footnotesize Schleifenabbruch}{DeepPink}
	\end{tikzpicture}
\end{minipage}


\section{Kopfgesteuerte Schleifen mit \texorpdfstring{\lstinline|while|}{while}}

\begin{myexample}
Folgendes Beispiel zeigt, wie ein \lstinline|while| mit \lstinline|break| zu einem einfachen \lstinline|while| mit Ausführungs-Bedingung vereinfacht werden kann. Beide Codes machen dasselbe. Die Schleife wird solange ausgeführt, wie die Ausführungs-Bedingung wahr ist. Die Abbruchskondition wird vor jeder neuen Schleifenausführung überprüft und die Schleife wird nur dann ausgeführt, wenn die Ausführungs-Bedingung noch wahr ist. Beim linken Code verwenden wir nicht eine Ausführungs-Bedingung sondern eine Abbruch-Bedingung.

\begin{center}
	\begin{minipage}{.48\textwidth}
		\begin{lstlisting}[escapechar=!]
				import turtle as t
				
				def spirale(seite, add, max_seite):
				while True:               !\tikzmark{fromStart}!
					if seite > max_seite:
						break             !\tikzmark{fromEnd}!
					t.fd(seite)
					t.rt(90)
					seite+=add
					
				spirale(10, 10, 100)
			\end{lstlisting}
	\end{minipage}
	\begin{minipage}{.48\textwidth}
		\begin{lstlisting}[escapechar=!]
				import turtle as t
				
				def spirale(seite, add, max_seite):
					!\tikzmark{toStart}!!\tikzmark{toEnd}!while seite > max_seite:
					t.fd(seite)
					t.rt(90)
					seite+=add
				
				spirale(10, 10, 100)
			\end{lstlisting}
	\end{minipage}
	\begin{tikzpicture}[overlay, remember picture,
			every edge/.append style = { ->, thick, >=stealth, red!70},
			every node/.style={draw=red!50,fill=red!50!white,text=black,rounded corners}]

		\drawBrace{0em}{fromStart}{fromEnd}{}{.5}{}{}
		\drawBrace{-.5em}{toStart}{toEnd}{}{.5}{mirror}{}
		% arrow pointing from one code to other
		\draw[->, style={thick, >=stealth, red!70}] (fromStartfromEnd-coord-arrow) to (toStarttoEnd-coord-arrow);
	\end{tikzpicture}
\end{center}
\end{myexample}


\begin{myattention}[Endlos-Schleifen]
	Beachten Sie folgende Punkte:
	\begin{itemize}
		\item Passen Sie auf, dass Sie keine unendlichen Schleifen produzieren! In diesem Fall kann das Programm, auf dem Python läuft, hängen bleiben. Vergessen Sie daher nie die Abbruchkondition klar zu formulieren!
		\item Ein \lstinline|while True:| (unendliche Schleife) ohne \lstinline|break| kann zum Absturz des Programms führen \emoji{face-with-spiral-eyes}
		\item Je nachdem kann das auch in einem \lstinline|while| mit einer Kondition passieren, sofern die Bedingung nach dem \lstinline|while| so geschrieben ist, dass sie immer wahr (\lstinline|True|) ist (s. \cref{subsec:semantische-fehler} zu semantischen Fehlern).
		\item \textbf{Speichern Sie regelmässig Ihre Aufgaben!}
		\item Falls das Programm VSCodium hängenbleibt: Beenden mit \keys{Ctrl+Alt+\backdel} (Windows), bzw. Activity Monitor unter MacOS
	\end{itemize}
\end{myattention}

\begin{mysubmission}
	Schreiben Sie eine Funktion \lstinline|verdreifache_bis_ueber1Mio(zahl)|, welche eine Zahl \texttt{zahl} so lange immer wieder verdreifacht, bis \texttt{zahl} erstmals grösser als 1'000'000 ist. Dabei sollen alle Zwischenresultate in der Konsole ausgegeben werden. Am Schluss soll ausserdem die Anzahl Verdreifachungen ausgedruckt werden.
	\begin{myanswer}
		\lstinputlisting[language=Python,caption=\texttt{while\_verdreifache.py},label=listing:while-verdreifache]{GrundlagenInfo/00_Programmieren/Skript/Code/K04_Logic/while_verdreifache.py}
	\end{myanswer}
\end{mysubmission}


\begin{myexercise}
	Entwickeln Sie eine Funktion \lstinline|def quadgleich(a, b, c)|, der quadratische
	Gleichungen der Form $ax^2 + bx + c = 0$ löst. Der Befehl soll zuerst $d = b^2 - 4ac$
	ausrechnen und abhängig vom Wert von d keine, eine oder zwei Lösungen aus-
	geben
\end{myexercise}


\begin{myexercise}
	Entwickeln Sie eine Funktion, dem eine Zahl $x > 1$ als Parameter übergeben wird. Aus $x$ wird nun eine Folge von Zahlen generiert und ausgegeben. Dabei wird folgende Regel angewendet: Wenn $x$ durch zwei teilbar ist, ist die nächste Zahl $x / 2$ . Wenn $x$ nicht durch zwei teilbar ist, ist die nächste Zahl $3 \cdot x + 1$ . Dieser Prozess wird wiederholt, solange die neu berechnete Zahl grösser als 1 ist.
\end{myexercise}

\begin{mychallenge}[Weizenkornlegende]
	\begin{figure}[H]
		\centering
		\includegraphics[width=0.3\linewidth]{GrundlagenInfo/09_Tabellenkalkulation/Screenshots/Sissa}
	\end{figure}
	Sissa ibn Dahir lebte angeblich im dritten oder vierten Jahrhundert in Indien und gilt Legenden zufolge als der Erfinder des Schachspiels.

	Der indische Herrscher Shihram tyrannisierte seine Untertanen und stürzte sein Land in Not und Elend. Um die Aufmerksamkeit des Königs auf seine Fehler zu lenken, ohne seinen Zorn zu entfachen, schuf der weise Brahmane Sissa ein Spiel, in welchem der König als wichtigste Figur ohne Hilfe anderer Figuren und Bauern nichts ausrichten kann. Der Unterricht im Schachspiel machte auf den Herrscher Shihram einen starken Eindruck. Er wurde milder und liess das Schachspiel verbreiten, damit alle davon Kenntnis nehmen.

	Um sich für die anschauliche Lehre von Lebensweisheit und zugleich Unterhaltung zu bedanken, gewährte er dem Brahmanen einen freien Wunsch. Dieser wünschte sich Weizenkörner: Auf das erste Feld eines Schachbretts wollte er ein Korn, auf das zweite Feld das Doppelte, also zwei, auf das dritte wiederum die doppelte Menge, also vier und so weiter.

	Sie sollen die Menge Weizenkörner, welche Sissa vom Herrscher gefordert hat, berechnen. Gehen Sie folgendermassen vor:

	Auf dem ersten Schachfeld liegt 1 Reiskorn, auf dem zweiten Feld liegen 2 Reiskörner, auf dem dritten Feld liegen 4 Körner usw.

	Allgemein liegen auf dem \(n+1\)-ten Schachfeld genau doppelt soviele Körner wie auf dem \(n\)-ten Schachfeld. Unser Schachbrett habe \(n\)-Felder, wobei \(n \leq 67\). Wie viele Reiskörner würden in dieser Situation insgesamt auf dem Schachfeld liegen (angenommen so viel Reis hätte Platz)?

	Schreiben Sie eine Funktion \lstinline|def reis(n)|, welche die gesuchte Anzahl Reiskörner in Abhängigkeit der Anzahl \(n\) der Felder berechnet und per \lstinline|return| zurückgibt.

	\begin{itemize}
		\item \lstinline|reis(3)| sollte 7 ausgeben.
		\item \lstinline|reis(8)| sollte 255 ausgeben.
		\item \lstinline|reis(64)| sollte 18446744073709551615 ausgeben.
	\end{itemize}

	\begin{myanswer}
		\lstinputlisting[language=Python,caption=\texttt{reis.py},label=listing:reis]{GrundlagenInfo/00_Programmieren/Skript/Code/K04_Logic/reis.py}
	\end{myanswer}

\end{mychallenge}




\subsection{Anwendungs-Aufgaben}

Folgende Beispiele sollen die meisten der bisher gelernten Programmier-Konzepte konkret veranschaulichen. Stellen Sie sich vor, Sie sind SportlerIn und möchten Ihren Kalorienbedarf berechnen, um sich für auf einen kommenden Wettkampf optimal zu ernähren. Hierzu wollen zunächst Sie Ihren täglichen (theoretischen) Kalorienbedarf berechnen. Ihr Kalorienbedarf berechnet sich wie folgt:
\begin{enumerate}
	\item Ihr \textbf{Grundumsatz} (englisch \gls{BMR}): Dies ist ihr Grundbedarf, also die Anzahl Kalorien, welche Sie theoretisch benötigen, falls Sie sich gar nicht bewegen.
	\item Ihr \textbf{Leistungsumsatz}, d.h., zusätzliche Energie, die Sie bei Aktivitäten wie Spazieren, Radfahren, Joggen usw. verbrennen.
\end{enumerate}

Beide Komponenten sind schwierig abzuschätzen. Sie möchten jedoch eine erste Einschätzung Ihrer \gls{BMR} haben, indem sie einige bekannte Formeln auf sich selber anwenden und diese vergleichen.

\begin{mysubmission}\label{ex:bmr-simple}
	Schreiben Sie eine Python-Funktion, um den Grundumsatz anhand der Harris-Benedict-Formel berechnen und geben Sie das Resultat auf der Konsole aus.
	\begin{enumerate}
		\item Für Männer:
		      \begin{align*}
			      \text{BMR} = & 88.362 +                                   \\
			                   & (13.397 \times \text{Gewicht in kg}) +     \\
			                   & (4.799 \times \text{Körpergrösse in cm}) - \\
			                   & (5.677 \times \text{Alter in Jahren})
		      \end{align*}
		\item Für Frauen:
		      \begin{align*}
			      \text{BMR} = & 447.593  +                                 \\
			                   & (9.247 \times \text{Gewicht in kg}) +      \\
			                   & (3.098 \times \text{Körpergrösse in cm}) - \\
			                   & (4.330 \times \text{Alter in Jahren})
		      \end{align*}
	\end{enumerate}
	\begin{myanswer}
		\lstinputlisting[language=Python]{GrundlagenInfo/00_Programmieren/Skript/Code/K04_Logic/grundumsatz.py}
	\end{myanswer}
\end{mysubmission}

Nun möchten wir zum Code, den Sie in \cref{ex:bmr-simple} geschrieben haben, zum Grundumsatz auch noch unsere Aktivitäten hinzufügen, allerdings können wir nun nicht mehr auf die Variable \texttt{bmr} zurückgreifen. Wie können wir das ändern? Indem wir sie an das Programm zurückgeben und speichern. Wichtige Änderungen gegebüber den Lösungs-Code aus \cref{ex:bmr-simple} sind in \colorbox{CornflowerBlue}{blau} umrandet.

\begin{minipage}{\linewidth}
	\begin{lstlisting}
		def grundumsatz(gewicht, groesse, alter, geschlecht):
			if geschlecht == 'mann':
				bmr = 88.362 + (13.397 * gewicht) + (4.799 * groesse) - (5.677 * alter)
			elif geschlecht == 'frau':
				bmr = 447.593 + (9.247 * gewicht) + (3.098 * groesse) - (4.330 * alter)
			
			(*@\colorbox{CornflowerBlue}{return b\tikzmark{arrowFrom}mr}@*) # gib den bmr an das Programm zurück
		
		# Beispiel:
		gewicht = 75  # kg
		groesse = 180  # cm
		alter = 30  # jahre
		geschlecht = 'mann'
		
		# berechne grundumsatz(*@\colorbox{CornflowerBlue}{und speichere das Resultat in einer neuen Variable resultat}@*)
		(*@\colorbox{CornflowerBlue}{resu\tikzmark{arrowTo}ltat = }@*) grundumsatz(gewicht, groesse, alter, geschlecht)
		(*@\colorbox{CornflowerBlue}{print("Der Grundumsatz beträgt:", round(resultat, 2), "Kalorien")}@*)
	\end{lstlisting}
\end{minipage}

\begin{tikzpicture}[overlay,remember picture]
	% Wert zurückgeben
	\draw[red,-stealth] (arrowFrom.north) to[bend left] node[right, fill=RoyalBlue,text=white,rounded corners] {Wert Zurückgeben} (arrowTo);
\end{tikzpicture}

Nun haben wir eine erste Einschätzung des \gls{BMR}. Allerdings gibt es auch noch weitere Formeln, die wir gerne berechnen möchten, um einen Vergleich der verschiedenen Methoden zu erhalten.

\begin{mysubmission}\label{ex:bmr}
	Schreiben Sie eine weitere Funktionen, um den \gls{BMR} anhand der Mifflin-St-Jeor-Gleichung und der Katch-McArdle-Formel zurückzugeben und berechnen Sie den Durchschnitt aller drei Formeln.
	\begin{enumerate}
		\item Mifflin-St-Jeor-Gleichung:
		      \begin{enumerate}
			      \item Männer:
			            \begin{align*}
				            \text{BMR} & = 10 \times \text{Gewicht (kg)}        \\
				                       & \quad + 6.25 \times \text{Grösse (cm)} \\
				                       & \quad - 5 \times \text{Alter (Jahre)}  \\
				                       & \quad + 5
			            \end{align*}
			      \item Frauen:
			            \begin{align*}
				            \text{BMR} & = 10 \times \text{Gewicht (kg)}        \\
				                       & \quad + 6.25 \times \text{Grösse (cm)} \\
				                       & \quad - 5 \times \text{Alter (Jahre)}  \\
				                       & \quad - 161
			            \end{align*}
		      \end{enumerate}
		\item Katch-McArdle-Formel:
		      \begin{enumerate}
			      \item Grundformel:
			            \begin{align*}
				            \text{BMR} & = 370                                  \\
				                       & \quad + (21.6 \times \text{FFM in kg})
			            \end{align*}

			      \item Fett-freie Masse (in kg):
			            \begin{align*}
				            \text{FFM} & = \text{Körpergewicht (kg)}                \\
				                       & \quad \times (1 - \text{Körperfettanteil})
			            \end{align*}

		      \end{enumerate}

	\end{enumerate}

	\begin{myanswer}
		\lstinputlisting{GrundlagenInfo/00_Programmieren/Python/bmr_functions/bmr.py}
	\end{myanswer}
\end{mysubmission}

Nun haben wir eine etwas verlässlichere Einschätzung unseres Grundbedarfs. Zu diesem können wir nun auch noch die Leistungsenergie hinzufügen, um den gesamten Energiebedarf zu berechnen.

\begin{mysubmission}\label{ex:leistungsumsatz}
	Berechnen Sie Ihre Leistungsenergie anhand der Tabelle \ref{tab:kcal}, indem Sie zwei Listen erstellen:
	\begin{enumerate}
		\item Eine Liste für die Zeit, während der Sie täglich einer Aktivität nachgehen
		\item Eine Liste mit dem Kalorienverbrauch pro Minute für jede dieser Aktivitäten
	\end{enumerate}

	\begin{table}[H]
		\centering
		\begin{tabular}{c|*{10}{c}}
			\textbf{Gewicht (kg)}    & 60   & 65   & 70   & 75   & 80   & 85   & 90   & 95   & 100  & 105  \\
			\midrule\midrule
			\textbf{Spazieren}       & 4.5  & 5.1  & 5.4  & 5.7  & 6.0  & 6.4  & 6.7  & 7.3  & 8.0  & 9.0  \\
			\midrule
			\textbf{Schnelles Gehen} & 5.5  & 6.3  & 7.0  & 7.5  & 8.0  & 8.6  & 9.5  & 10.0 & 10.6 & 11.3 \\
			\midrule
			\textbf{Fahrradfahren}   & 6.5  & 7.5  & 8.0  & 9.0  & 9.5  & 10.3 & 11.0 & 11.7 & 12.5 & 13.6 \\
			\midrule
			\textbf{Schwimmen}       & 8.0  & 9.2  & 10.0 & 10.8 & 11.5 & 12.5 & 13.7 & 14.4 & 15.4 & 16.5 \\
			\midrule
			\textbf{Rudern}          & 11.0 & 13.0 & 14.3 & 15.0 & 16.5 & 17.5 & 19.0 & 20.5 & 21.8 & 23.5 \\
			\midrule
			\textbf{Jogging}         & 13.0 & 14.5 & 16.0 & 17.5 & 19.0 & 20.0 & 22.0 & 23.5 & 24.5 & 26.5 \\
			\midrule
			\textbf{Laufen}          & 16.0 & 18.5 & 20.0 & 22.0 & 23.5 & 25.0 & 27.5 & 29.0 & 31.0 & 33.5 \\
			\midrule
			\textbf{Sprint}          & 19.0 & 21.5 & 24.0 & 26.0 & 28.0 & 30.0 & 32.5 & 34.7 & 35.6 & 39.0 \\
		\end{tabular}
		\caption{Kalorienverbrauch für unterschiedliche Aktivitäten, pro Minute}
		\label{tab:kcal}
	\end{table}
	Wenn z.B. eine 80 kg schwere Person 1 Stunde lang rudert und 20 Minuten spaziert wären die Listen wie folgt:
	\begin{lstlisting}
		l_zeit = [60, 20] # Zeit in Minuten
		l_kcal = [16.5, 6] # Kalorien pro Aktivität
	\end{lstlisting}
	Schreiben Sie eine Python-Funktion \lstinline|berechne_leistungsumsatz|, um Ihren Leistungsumsatz zu berechnen. Das erwartete Resultat für das Beispiel hier wäre: \lstinline|1110| (= Summe von \lstinline|[990, 120]|). Das Resultat soll an das Hauptprogramm zurückgegeben und in einer Variable \texttt{kalorienverbrauch} gespeichert werden.
	\begin{myanswer}
		\lstinputlisting{GrundlagenInfo/00_Programmieren/Python/bmr_functions/leistungsumsatz.py}
	\end{myanswer}

\end{mysubmission}

\begin{mysubmission}
	Schreiben Sie nun eine weitere Funktion, die Ihren gesamten Energieumsatz berechnet, als Summe Ihres BMRs, den Sie in \cref{ex:bmr} berechnet haben (durchschnitt der drei Formeln) und Ihres Leistungsumsatzes, den Sie in \cref{ex:leistungsumsatz} berechnet haben. Die Funktion ist extrem kurz und umfässt nur eine Zeile.
	\begin{myanswer}
		Angenommen, die bisher geschriebenen Funktionen wurden in den Dateien \texttt{bmr.py} und \texttt{leistungsumsatz.py} gespeichert, so können Sie die Funktion \lstinline|def total(bmr, leistungsumsatz)| wie folgt schreiben:
		\lstinputlisting{GrundlagenInfo/00_Programmieren/Python/bmr_functions/total.py}
	\end{myanswer}
\end{mysubmission}




\begin{mycaution}
	Die Zahlen zu Energiebedarf und Kalorien, die Sie im Rahmen dieser Übungen berechnet haben, sollten Sie mit Vorsicht geniessen, aus folgenden Gründen:
	\begin{itemize}
		\item Ihr realer Energiebedarf kann signifikant von Ihrem berechneten Energiebedarf abweichen. Viele weitere Faktoren, die nicht in den Formeln enthalten sind, können diesen beeinflussen, beispielsweise Ihre Köpertemperatur, Ihre Fitness, Ihre Muskelmasse, sowie Ihre \gls{NEAT}, wobei Letzteres Energie bezeichnet, die Sie durch Bewegungen, die nicht Sport sind, verbrauchen (beispielsweise durch ``Zappeln'' aber auch kurze Spaziergänge und alltägliche Aktivitäten).
		\item Sie sollten sich bezüglich konsumierter Kalorien vor Augen halten, dass ``zu viele'' konsumierte Kalorien nicht automatisch mit einer Gewichtszunahme verbunden sind. In einem Kilo Körperfett stecken ca. 8000 Kilokalorien, wobei der Köper über verschiedene Mechanismen verfügt, um ein stabiles Körpergewicht zu halten, etwa die Erhöhung oder Senkung der Körpertemperatur im Falle eines kurzfristigen Kalorien-Überschusses, respektive -Defizits. Allerdings kann es interessant sein, zu wissen, dass einige, nicht sehr sättigende Lebensmittel besonders viele Kalorien enthalten –– wie beispielsweise hochverarbeitete Snacks oder Süssgetränke.
	\end{itemize}
\end{mycaution}
